## 根据类型生成【HTML】

- 通过系统提示词指定生成内容为HTML或HTML、Js、css【枚举类区分】
- 处理两种输出情况、解析并返回【策略模式】【可扩展多种】
- 将两种情况保存到本地【模板方法】【可扩展多种】
- 选择策略或者选择方法的方法可采用选择路由【switch-case（不太符合开闭原则修改了较为核心的业务代码）、**工厂+注册表**Map、等方法】

> | 方式            | 新增策略需要做的变更                     | 是否修改已有代码 | 是否符合开闭原则 |
> | :-------------- | :--------------------------------------- | :--------------- | :--------------- |
> | **条件路由**    | 在 `switch` 中添加新的 `case`            | ✅ 修改现有代码   | ❌ 不符合         |
> | **工厂+注册表** | 在初始化块中添加一行 `registry.put(...)` | ❌ 不修改核心路由 | ✅ 符合           |
> | **依赖注入**    | 新建一个 `@Component` 类                 | ❌ 不修改任何代码 | ✅ 符合           |
> | **配置文件**    | 在配置文件中添加一条键值对               | ❌ 不修改任何代码 | ✅ 符合           |
> | **自注册**      | 新建一个带注解的类                       | ❌ 不修改任何代码 | ✅ 符合           |

## 游标查询

按阶段加载【根据字段顺序】，并且可能出现新插入的数据，不能直接查询。历史对话消息，按时间加载，并且可以联合id 索引

## 对话历史

每完成一次对话就插入到数据库